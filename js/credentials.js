const bucket = 'darkost-music-public'         // Bucket, that contains music.
const endpoint = 'storage.yandexcloud.net'    // Without leading protocol and trailing slash.
const accessKey = undefined                   // Keys to access Object Storage bucket. Optional.
const secretKey = undefined                   // It's strongly discouraged to place your secret data in plain text. This is only the example of implementation.
