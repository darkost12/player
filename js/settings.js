const BUCKET = 'darkost-music-public'           // Bucket, that contains music.
const ENDPOINT = 'storage.yandexcloud.net'      // Without leading protocol and trailing slash.
const ACCESS_KEY = undefined                    // Keys to access Object Storage bucket. Optional.
const SECRET_KEY = undefined                    // It's strongly discouraged to place your secret data in plain text. This is only the example of implementation.
